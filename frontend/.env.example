# Opcional: configura o cliente HTTP, mas NÃO ativa a integração da interface.
# Após implementar a ligação descrita em docs/HANDOFF.md:
NEXT_PUBLIC_API_URL=
# Exemplo: https://api.seudominio.com.br (sem /v1 no final)
# Nunca adicionar segredos OpenAI, Cakto ou banco em NEXT_PUBLIC_*.
